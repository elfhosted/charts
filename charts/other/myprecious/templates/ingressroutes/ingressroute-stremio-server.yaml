{{ if or .Values.stremioserver.enabled .Values.stremioserverpia.enabled .Values.stremioservergluetun.enabled }}
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  {{ if .Values.stremioserver.enabled }}
  name: {{ .Release.Name }}-stremio-server{{ .Values.stremioserver.SECRET_STRING | default "nothing" }}.elfhosted.com
  {{ elseif .Values.stremioserverpia.enabled }}
  name: {{ .Release.Name }}-stremio-server{{ .Values.stremioserverpia.SECRET_STRING | default "pia" }}.elfhosted.com
  {{ elseif .Values.stremioservergluetun.enabled }}
  name: {{ .Release.Name }}-stremio-server{{ .Values.stremioservergluetun.SECRET_STRING | default "gluetun" }}.elfhosted.com
  {{ end }}
spec:
  routes:
    {{ if .Values.stremioserver.enabled }}
    - match: Host(`{{ .Release.Name }}-stremio-server-{{ .Values.stremioserver.SECRET_STRING }}.elfhosted.com`)
    {{ elseif .Values.stremioserverpia.enabled }}
    - match: Host(`{{ .Release.Name }}-stremio-server-{{ .Values.stremioserverpia.SECRET_STRING }}.elfhosted.com`)
    {{ elseif .Values.stremioservergluetun.enabled }}
    - match: Host(`{{ .Release.Name }}-stremio-server-{{ .Values.stremioservergluetun.SECRET_STRING }}.elfhosted.com`)
    {{ end }}
      kind: Rule
      services:
        - name: stremio-server
          kind: Service
          port: 11470
      middlewares:
        - name: headers-add-cors-for-stremio
          namespace: traefik
        - name: error-pages
          namespace: traefik
{{ end }}