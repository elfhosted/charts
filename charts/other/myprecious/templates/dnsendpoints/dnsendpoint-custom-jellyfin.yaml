{{- if .Values.jellyfincustomdomain.enabled }}
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: {{ .Release.Name }}-jellyfin.elfhosted.com
spec:
  endpoints:
  - dnsName: {{ .Release.Name }}-jellyfin.elfhosted.com
    recordTTL: 180
    recordType: CNAME
    targets:
    - "traefik-ingress.elfhosted.com"
{{ end }}  