{{ if .Values.smbencrypteda.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-smb-encrypted-a
  namespace: csi-rclone
type: Opaque
stringData:
  remote: smb-encrypted-a
  remotePath: # intentionally left blank, drive doesn't need it
  vfs-cache-mode: full
  poll-interval: 10s
  drive-pacer-min-sleep: 10ms
  drive-pacer-burst: "200"
  vfs-cache-max-size: 800G
  bwlimit-file: 10M
  timeout: 1h  
  configData: |
    [smb-a]
    type = smb
    client_id: {{ .Values.smbencrypteda.host }}
    client_secret: {{ .Values.smbencrypteda.pass }}
    user: {{ .Values.smbencrypteda.user }}

    [smb-encrypted-a]
    type = crypt
    remote = smb-a:{{ .Values.smbencrypteda.share }}/{{ .Values.smbencrypteda.encrypted_root | default }}
    filename_encryption = off
    password = {{ .Values.smbencrypteda.password }}
    password2 = {{ .Values.smbencrypteda.password2 | default }}
{{ end }}