 # This just exists to trigger reloader to restart apps which need the rclones/storageboxes refreshed
apiVersion: v1
kind: ConfigMap
metadata:
  name: storage-changed
data:
  SHAs: |
     {{- tpl (toYaml .Values.b2) . | sha256sum -}}    
     {{- tpl (toYaml .Values.gdrivea) . | sha256sum -}}
     {{- tpl (toYaml .Values.gdriveb) . | sha256sum -}}
     {{- tpl (toYaml .Values.gdrivec) . | sha256sum -}}
     {{- tpl (toYaml .Values.gdrived) . | sha256sum -}}
     {{- tpl (toYaml .Values.premiumize) . | sha256sum -}}    
     {{- tpl (toYaml .Values.onedrivea) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonegdriveencrypteda) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonegdriveencryptedb) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonegdriveencryptedc) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountputio) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountpikpak) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountalldebrid) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountrealdebridzurg) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountssha) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonemountwebdava) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonesmbencrypteda) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonestorageboxencrypteda) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonestorageboxencryptedb) . | sha256sum -}}
     {{- tpl (toYaml .Values.rclonestorageboxencryptedc) . | sha256sum -}}
     {{- tpl (toYaml .Values.smbmounta) . | sha256sum -}}
     {{- tpl (toYaml .Values.smbmountb) . | sha256sum -}}
     {{- tpl (toYaml .Values.smbmountc) . | sha256sum -}}
     {{- tpl (toYaml .Values.storageboxa) . | sha256sum -}}
     {{- tpl (toYaml .Values.storageboxb) . | sha256sum -}}
     {{- tpl (toYaml .Values.storageboxc) . | sha256sum -}}