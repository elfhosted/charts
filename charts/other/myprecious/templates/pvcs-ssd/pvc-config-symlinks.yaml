apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: symlinks
  annotations:
    helm.sh/resource-policy: delete
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-filesystem
