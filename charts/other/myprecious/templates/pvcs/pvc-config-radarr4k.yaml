{{ if .Values.radarr4k.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-radarr4k
  annotations:
    helm.sh/resource-policy: keep
  labels:
    velero.io/snapshot-volume: "true"
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 2Gi
  storageClassName: ceph-filesystem
{{ end }}