{{ if .Values.rapidleech.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-rapidleech
  annotations:
    helm.sh/resource-policy: keep
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Mi
  storageClassName: ceph-filesystem
{{ end }}