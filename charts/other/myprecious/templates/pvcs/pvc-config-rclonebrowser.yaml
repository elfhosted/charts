apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-rclonebrowser
  annotations:
    helm.sh/resource-policy: keep  
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: ceph-filesystem
